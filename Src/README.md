XCOM-Mutators: Source Code
==========================

Unreal Script source code for XComMutator class to compile with UDK.

To write your own mutators, create a subclass, which extends XComMutator class.
